# logentries-dropwizard
Sample AppenderFactory to use Logentries.com with Dropwizard.

Primarily created to share with the LogEntries team for review; it's a little unclear how best to configure the LE appender via code rather than by logback.xml, which Dropwizard bypasses in favor of simplified YAML.
